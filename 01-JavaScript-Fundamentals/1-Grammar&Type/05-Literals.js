/****************
LITERALS
*****************/

let days = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday'];

console.log(days.toString())
//This is a method to turn arrays into one string

console.log(days.length)

let soup = {
    a: 'chicken noodle',
    b: 'tomato',
    c: 'beef and barley'
};
/* objects are key value pairs, which just means in an object there are keys (what the name of this part of the object is),
and a value for that key. */

console.log(soup.a)
/*This is called dot notation for an object. This is how we can dig through an object
to get back data we want. */

let car = "Honda";

let dec = 10.2;
